#
# MEMO
#

Démarrage d'une session dans le container:
- docker container exec -it beedule /bin/sh

Génération site développement:
- hugo -d /volshare/foired --environment DEV --cleanDestinationDir

Génération site production:
- hugo -d /volshare/foirep --cleanDestinationDir
